# queues

Will own the RabbitMQ connection and exchange/queue topology defined in the
architecture proposal (exchange.deposit, exchange.wallet, ...). Kept empty in
Phase 0 — only the foundation module is declared so the dependency graph is
stable before any business workflow is implemented.
