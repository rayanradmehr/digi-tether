# health module

Infrastructure/liveness endpoint, not a business domain module. Serves as the
reference example for the mandated per-module layout (Controller, Service —
omitted here since Terminus provides it, Tests, README) and full Swagger
documentation that every future business module must replicate.
