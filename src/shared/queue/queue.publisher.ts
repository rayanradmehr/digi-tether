// QueuePublisher — typed service to publish messages to RabbitMQ exchanges
// publish(exchange, routingKey, payload) — fire and forget
// publishWithConfirm(exchange, routingKey, payload) — awaits broker ack
